using System;

public class Class1
{
    public Class1()
    {
    }

    @override
    pubclic double[] ComputeMean()
    {
        //Not supported exept for singletons.
        return points.count() == 1 ? points.get(1) : null;
        // 
    }
}